const USERS_COLLECTION = "users";
const AUTH_COOKIE_NAME = "shop_agentic_session";
const AUTH_COOKIE_MAX_AGE_MS = 60 * 60 * 1000;

module.exports = {
  USERS_COLLECTION,
  AUTH_COOKIE_NAME,
  AUTH_COOKIE_MAX_AGE_MS,
};
