const ORDERS_COLLECTION = "orders";
const EVENTS_COLLECTION = "events";
const GROUPS_COLLECTION = "groups";

const DEFAULT_VAT_RATE = 0.1; // 10% VAT theo luật GTGT VN hiện hành (2026)

module.exports = {
  ORDERS_COLLECTION,
  EVENTS_COLLECTION,
  GROUPS_COLLECTION,
  DEFAULT_VAT_RATE,
};
